@model CityLibraryFund.Models.Entities.User
@{
    ViewBag.Title = "Details of user " + Model.Name;
}
<h2>User @Model.Name</h2>
<div class="form-group">
    <dl class="dl-horizontal">
        <dt>Identifier</dt>
        <dd>@Model.Id</dd>
    </dl>
</div>
<div class="form-group">
    <a asp-action="AdministrateUsers" asp-controller="Clients">Back to users list</a>
</div>